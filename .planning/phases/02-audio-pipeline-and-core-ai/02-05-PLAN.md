---
phase: 02-audio-pipeline-and-core-ai
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified: []
autonomous: false
requirements:
  - AUDP-01
  - AUDP-02
  - AUDP-03
  - AUDP-04
  - AIRC-01
  - AIRC-02
  - AIRC-03
  - AIRC-04
  - AIRC-05
  - AIRC-06
  - AIRC-07
  - AIRC-08
  - RIWY-03

must_haves:
  truths:
    - "Full recording-to-results flow works on a physical device"
    - "Word highlights appear correctly on mushaf text"
    - "Offline queue saves and uploads on reconnect"
    - "Server rejects invalid audio and unsupported riwayah"

user_setup:
  - service: "FastAPI inference server"
    why: "AI model inference for recitation analysis"
    env_vars:
      - name: SUPABASE_JWT_SECRET
        source: "Supabase Dashboard -> Settings -> API -> JWT Secret"
      - name: EXPO_PUBLIC_INFERENCE_URL
        source: "Your FastAPI server URL (e.g., https://api.lawh.app or http://localhost:8000 for local testing)"
    dashboard_config:
      - task: "Download and convert Whisper model"
        location: "Run: cd lawh-api && python scripts/convert_model.py"
      - task: "Start FastAPI server with GPU"
        location: "Run: cd lawh-api && uvicorn app.main:app --host 0.0.0.0 --port 8000"
---

<objective>
End-to-end verification checkpoint: confirm the complete audio pipeline works on a physical device — recording, upload, inference, word-level highlights, score display, and offline queue behavior.

Purpose: Phase 2 spans two subsystems (mobile + server) that must work together. Device testing is essential because audio recording, file system access, and network behavior differ significantly from simulators.
Output: Human-verified confirmation that the full recitation analysis flow works correctly.
</objective>

<execution_context>
@/Users/yousef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yousef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-audio-pipeline-and-core-ai/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end audio pipeline verification on physical device</name>
  <files>N/A - verification only</files>
  <action>
    Present the verification checklist to the user. The complete audio pipeline has been built across Plans 01-04:
    - FastAPI server: JWT auth, audio validation, Whisper ASR, passage detection, word-level diff, scoring, Supabase persistence
    - Mobile app: expo-audio-stream recording, recording state machine, offline queue with auto-upload, inference service, RecordButton, WaveformVisualizer, RecitationOverlay with inline word highlights, ResultsBottomSheet with accuracy score

    Pre-requisites (one-time setup):
    1. Ensure FastAPI server is running with the Whisper model loaded (see user_setup)
    2. Set EXPO_PUBLIC_INFERENCE_URL in lawh-mobile/.env
    3. Set SUPABASE_JWT_SECRET in lawh-api/.env

    Test 1 - Happy path recording (online):
    1. Open the app on a physical device, sign in
    2. Navigate to a mushaf page (e.g., Al-Fatiha, page 1)
    3. Tap the mic button in the footer
    4. Verify: mic button turns red and pulses, waveform visualizer appears above footer with animated bars and elapsed time counter
    5. Recite Al-Fatiha (or a few ayahs from the current page) for 10-15 seconds
    6. Tap the mic/stop button to stop recording
    7. Verify: progress overlay shows "Uploading..." then "Analyzing..."
    8. Verify: after analysis completes, word-level color highlights appear on the mushaf text (green=correct, red=wrong, orange=tashkeel, gray=skipped)
    9. Verify: bottom sheet appears with accuracy score (percentage ring), word count breakdown, and "Try Again" / "Done" buttons
    10. Tap "Done" -- verify highlights clear and mushaf returns to normal reading

    Test 2 - Try Again flow:
    1. Record and get results (same as Test 1)
    2. Tap "Try Again" in the bottom sheet
    3. Verify: highlights clear, recording starts immediately

    Test 3 - Offline queue:
    1. Enable airplane mode on device
    2. Tap mic and record a short recitation (5+ seconds)
    3. Stop recording
    4. Verify: toast appears "Saved -- will analyze when online"
    5. Disable airplane mode
    6. Verify: recording auto-uploads and results appear

    Test 4 - Error handling:
    1. Record a very short clip (~1 second) -- should get "too short" error
    2. Verify: error message shown with expandable detail
    3. Verify: "Retry" and "Save for later" buttons are available

    Test 5 - Server validation (optional, via curl):
    - Send invalid audio format: expected 422 about audio format
    - Send unsupported riwayah (warsh): expected 422 "not yet supported"
  </action>
  <verify>Human confirms all 5 test scenarios pass on physical device</verify>
  <done>Full recording-to-results flow verified on physical device. Word highlights render correctly. Offline queue works. Error handling is user-friendly.</done>
</task>

</tasks>

<verification>
Human verification of end-to-end flow on physical device. All 5 test scenarios pass.
</verification>

<success_criteria>
- Recording produces audio that the server can process
- Passage detection correctly identifies the recited Quran passage
- Word-level highlights appear on the correct words with appropriate colors
- Accuracy score is reasonable and reflects actual recitation quality
- Offline queue saves recording and auto-uploads on reconnect
- Error handling shows user-friendly messages with retry options
- Server rejects invalid audio format and unsupported riwayah
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-pipeline-and-core-ai/02-05-SUMMARY.md`
</output>
