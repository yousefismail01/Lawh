---
phase: 02-audio-pipeline-and-core-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lawh-api/requirements.txt
  - lawh-api/requirements-dev.txt
  - lawh-api/pytest.ini
  - lawh-api/tests/conftest.py
  - lawh-api/app/core/config.py
  - lawh-api/app/core/auth.py
  - lawh-api/app/core/models.py
  - lawh-api/app/main.py
  - lawh-api/app/services/audio_validator.py
  - lawh-api/app/schemas/inference.py
  - lawh-api/tests/test_auth.py
  - lawh-api/tests/test_audio_validation.py
  - lawh-api/tests/test_riwayah.py
  - lawh-api/Dockerfile
  - lawh-api/scripts/convert_model.py
autonomous: true
requirements:
  - AIRC-07
  - AIRC-08
  - AUDP-04
  - RIWY-03

must_haves:
  truths:
    - "FastAPI validates Supabase JWT and extracts user_id from verified claims on every request"
    - "Server rejects invalid audio format (wrong sample rate, channels, encoding) with 422"
    - "Server rejects unsupported riwayah with 422 error (not silent Hafs fallback)"
    - "Server handles inference requests without GPU or model loading errors"
  artifacts:
    - path: "lawh-api/app/core/auth.py"
      provides: "JWT validation FastAPI dependency"
      exports: ["get_current_user"]
    - path: "lawh-api/app/core/models.py"
      provides: "ML model singleton loader"
      exports: ["get_whisper_model"]
    - path: "lawh-api/app/services/audio_validator.py"
      provides: "WAV format validation"
      exports: ["validate_wav_format"]
    - path: "lawh-api/app/schemas/inference.py"
      provides: "Pydantic request/response models"
      exports: ["RecitationResponse", "WordResult", "WordStatus"]
    - path: "lawh-api/Dockerfile"
      provides: "CUDA-enabled Docker image"
      contains: "nvidia/cuda"
  key_links:
    - from: "lawh-api/app/core/auth.py"
      to: "lawh-api/app/core/config.py"
      via: "settings.supabase_jwt_secret"
      pattern: "settings\\.supabase_jwt_secret"
    - from: "lawh-api/app/core/models.py"
      to: "lawh-api/app/core/config.py"
      via: "settings.whisper_model_path"
      pattern: "settings\\.whisper_model_path"
---

<objective>
Build the FastAPI server foundation: JWT authentication, audio format validation, Whisper model loading, Pydantic schemas, CUDA Dockerfile, and test infrastructure.

Purpose: Establishes all server-side contracts and security gates that the inference pipeline (Plan 02) builds upon. JWT auth and audio validation are security-critical and must be solid before wiring inference.
Output: Authenticated, validated FastAPI server ready to accept inference requests; full pytest test suite for auth, audio validation, and riwayah gating.
</objective>

<execution_context>
@/Users/yousef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yousef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-pipeline-and-core-ai/02-RESEARCH.md

<interfaces>
<!-- Existing FastAPI skeleton -->
From lawh-api/app/main.py:
```python
class Riwayah(str, Enum):
    hafs = "hafs"
    warsh = "warsh"
    qalun = "qalun"
    ad_duri = "ad_duri"

SUPPORTED_RIWAYAT = {Riwayah.hafs}

app = FastAPI(title="Lawh Inference API", ...)
```

From lawh-api/app/core/config.py:
```python
class Settings(BaseSettings):
    supabase_url: str
    supabase_service_key: str
    device: str = "cpu"  # "cuda" on EC2 with GPU
    class Config:
        env_file = ".env"

settings = Settings()
```

From lawh-api/requirements.txt:
```
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-dotenv==1.0.1
supabase==2.4.0
pydantic-settings==2.3.0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test infrastructure, requirements, and Pydantic schemas</name>
  <files>
    lawh-api/requirements.txt,
    lawh-api/requirements-dev.txt,
    lawh-api/app/core/config.py,
    lawh-api/app/schemas/inference.py,
    lawh-api/pytest.ini,
    lawh-api/tests/__init__.py,
    lawh-api/tests/conftest.py
  </files>
  <action>
    1. Update requirements.txt: add faster-whisper, PyJWT, python-multipart, librosa, soundfile, numpy.
    2. Create requirements-dev.txt: pytest, pytest-timeout, pytest-asyncio, httpx (for FastAPI test client).
    3. Update config.py Settings: add supabase_jwt_secret (str), whisper_model_path (str, default "./models/whisper-quran"), max_audio_duration_seconds (int, default 300), min_audio_duration_seconds (int, default 3).
    4. Create schemas/inference.py: Pydantic models:
       - WordStatus = Literal["correct", "wrong", "skipped", "added", "tashkeel_error"]
       - WordResult(BaseModel): position (int), expected (str), transcribed (str | None), status (WordStatus), ayah_key (str, format "surah:ayah")
       - RecitationResponse(BaseModel): surah_id (int), start_ayah (int), end_ayah (int), accuracy_score (float, 0-1), word_results (list[WordResult]), word_count_breakdown (dict with correct/wrong/skipped/added/tashkeel_error counts)
    5. Create pytest.ini with asyncio_mode=auto, timeout=30.
    6. Create tests/__init__.py (empty).
    7. Create tests/conftest.py with fixtures: mock_jwt_secret, valid_token (generate a real JWT for testing with PyJWT), expired_token, sample_wav_bytes (generate valid 16kHz mono WAV using soundfile in memory), invalid_wav_bytes (44.1kHz stereo), short_wav_bytes (1 second).
    8. Install dev dependencies: `pip install -r requirements-dev.txt -q`
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-api && pip install -r requirements-dev.txt -q && python -c "from app.schemas.inference import RecitationResponse, WordResult; print('Schemas OK')" && python -m pytest --co -q 2>&1 | head -5</automated>
  </verify>
  <done>Requirements installed. Pydantic schemas defined. pytest.ini configured. conftest.py has all shared fixtures (JWT tokens, WAV byte generators). Test infrastructure is ready for test authoring in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth, audio validation, riwayah gating with TDD tests</name>
  <files>
    lawh-api/app/core/auth.py,
    lawh-api/app/services/audio_validator.py,
    lawh-api/tests/test_auth.py,
    lawh-api/tests/test_audio_validation.py,
    lawh-api/tests/test_riwayah.py
  </files>
  <behavior>
    - test_valid_jwt: Valid Supabase JWT with sub claim returns user_id string
    - test_expired_jwt: Expired JWT raises HTTPException 401
    - test_invalid_jwt: Malformed token raises HTTPException 401
    - test_missing_sub: JWT without sub claim raises HTTPException 401
    - test_valid_wav: 16kHz mono 16-bit WAV passes validation
    - test_invalid_sample_rate: 44.1kHz WAV raises HTTPException 422
    - test_invalid_channels: Stereo WAV raises HTTPException 422
    - test_non_wav: MP3 or random bytes raises HTTPException 422
    - test_empty_audio: Zero-length audio raises HTTPException 422
    - test_too_short_audio: Audio shorter than 3 seconds raises HTTPException 422 (Whisper hallucination guard)
    - test_hafs_riwayah: Hafs riwayah is accepted
    - test_warsh_riwayah: Warsh riwayah returns 422 "not yet supported"
    - test_unsupported_riwayah_message: Error message lists currently supported riwayahs
  </behavior>
  <action>
    1. Create auth.py: FastAPI HTTPBearer dependency `get_current_user` that decodes JWT with PyJWT using settings.supabase_jwt_secret, audience="authenticated", algorithms=["HS256"]. Extract user_id from payload["sub"]. Raise 401 for expired, invalid, or missing sub.
    2. Create audio_validator.py: `validate_wav_format(audio_bytes: bytes)` function using soundfile to read WAV headers. Validates: is valid WAV, sample_rate == 16000, channels == 1, subtype is PCM_16. Also validates duration >= min_audio_duration_seconds and <= max_audio_duration_seconds. Raises HTTPException(422) with descriptive message on any failure.
    3. Write tests per behavior list above in test_auth.py, test_audio_validation.py, test_riwayah.py.
    4. For riwayah tests, create a simple test endpoint fixture that validates riwayah against SUPPORTED_RIWAYAT.
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-api && python -m pytest tests/test_auth.py tests/test_audio_validation.py tests/test_riwayah.py -x --timeout=30 -v</automated>
  </verify>
  <done>All 13+ tests pass. JWT auth validates correctly (valid/expired/invalid/missing-sub). Audio validator catches wrong sample rate, channels, non-WAV, empty, and too-short. Riwayah gating rejects unsupported with descriptive message.</done>
</task>

<task type="auto">
  <name>Task 3: Whisper model loader, CUDA Dockerfile, and conversion script</name>
  <files>
    lawh-api/app/core/models.py,
    lawh-api/Dockerfile,
    lawh-api/scripts/convert_model.py,
    lawh-api/app/main.py
  </files>
  <action>
    1. Create models.py: Singleton `get_whisper_model()` that lazy-loads faster_whisper.WhisperModel with settings.whisper_model_path, device=settings.device, compute_type="int8_float16" for CUDA or "int8" for CPU. Include a startup log message with model path and device. Add `get_whisper_model` as a FastAPI dependency.
    2. Update Dockerfile: Change base to `nvidia/cuda:12.4.1-cudnn-runtime-ubuntu22.04`. Install python3.11, pip. Keep existing COPY/install pattern. Add NVIDIA runtime env vars (NVIDIA_VISIBLE_DEVICES=all, NVIDIA_DRIVER_CAPABILITIES=compute,utility). Add a volume mount point for /app/models.
    3. Create scripts/convert_model.py: Script to convert tarteel-ai/whisper-base-ar-quran from HuggingFace format to CTranslate2 format using `ctranslate2.converters.TransformersConverter`. Takes --output_dir argument (default ./models/whisper-quran). Includes pip install instructions in docstring for ct2-transformers-converter.
    4. Update main.py: Add startup event that logs "Lawh Inference API starting" with device info. Import and include inference router (placeholder -- actual router created in Plan 02). Add `from app.core.models import get_whisper_model` to trigger model loading check on first request.
    5. Do NOT attempt to actually download or convert the model -- just create the infrastructure code.
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-api && python -c "from app.core.models import get_whisper_model; from app.core.config import settings; print('Model loader importable, settings:', settings.device)"</automated>
  </verify>
  <done>models.py exports get_whisper_model singleton. Dockerfile uses CUDA 12.4 base image with GPU support. convert_model.py script is ready to use. main.py includes startup logging and router wiring point.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x --timeout=30` passes all auth, audio validation, and riwayah tests
- `python -c "from app.core.auth import get_current_user"` imports without error
- `python -c "from app.core.models import get_whisper_model"` imports without error
- `python -c "from app.schemas.inference import RecitationResponse, WordResult, WordStatus"` imports without error
- Dockerfile contains nvidia/cuda base image
</verification>

<success_criteria>
- JWT validation correctly accepts valid tokens and rejects expired/invalid/missing-sub with 401
- Audio validator rejects wrong sample rate, channels, non-WAV, empty, and too-short with 422
- Riwayah gating rejects non-Hafs with 422 and descriptive error
- Whisper model loader is ready for GPU inference
- All Pydantic schemas defined for inference request/response contract
- pytest infrastructure established with fixtures for all test domains
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-pipeline-and-core-ai/02-01-SUMMARY.md`
</output>
