---
phase: 06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lawh-mobile/app/(main)/_layout.tsx
  - lawh-mobile/app/(main)/index.tsx
  - lawh-mobile/app/(main)/hub.tsx
  - lawh-mobile/app/(main)/contents.tsx
  - lawh-mobile/app/(main)/hifz.tsx
  - lawh-mobile/app/(main)/recite.tsx
  - lawh-mobile/app/(main)/review.tsx
  - lawh-mobile/app/(main)/profile.tsx
  - lawh-mobile/app/(main)/settings.tsx
  - lawh-mobile/app/_layout.tsx
  - lawh-mobile/hooks/useChromeToggle.ts
  - lawh-mobile/components/mushaf/PageNavigator.tsx
autonomous: true
requirements:
  - FSCR-01
  - FSCR-02
  - FSCR-06

must_haves:
  truths:
    - "App launches to mushaf screen with no tab bar visible"
    - "Chrome auto-hides after 5 seconds of inactivity"
    - "PageNavigator visibility is controlled externally (no internal timer)"
  artifacts:
    - path: "lawh-mobile/app/(main)/_layout.tsx"
      provides: "Stack navigator replacing tab layout"
      contains: "Stack"
    - path: "lawh-mobile/hooks/useChromeToggle.ts"
      provides: "Chrome visibility state with auto-hide timer"
      exports: ["useChromeToggle"]
    - path: "lawh-mobile/components/mushaf/PageNavigator.tsx"
      provides: "Controlled PageNavigator without internal timer"
      contains: "visible"
  key_links:
    - from: "lawh-mobile/app/_layout.tsx"
      to: "lawh-mobile/app/(main)/_layout.tsx"
      via: "Stack.Screen name='(main)'"
      pattern: "name.*main"
    - from: "lawh-mobile/app/_layout.tsx"
      to: "/(main)/"
      via: "AuthGate redirect"
      pattern: "main"
---

<objective>
Restructure the Expo Router layout from a 5-tab architecture to a mushaf-primary Stack layout, create the useChromeToggle hook for fullscreen toggle behavior, and refactor PageNavigator to controlled mode.

Purpose: This is the foundation for all Phase 6 work. The route structure change removes the tab bar from the mushaf experience, and the chrome toggle hook provides the unified visibility state that the overlay icons and PageNavigator will share.

Output: New (main) route group with Stack layout, useChromeToggle hook, controlled PageNavigator, placeholder screens for hub/contents/features.
</objective>

<execution_context>
@/Users/yousef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yousef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary/06-CONTEXT.md
@.planning/phases/06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lawh-mobile/app/_layout.tsx (current root layout):
```typescript
// AuthGate currently redirects to '/(tabs)/' -- must change to '/(main)/'
function AuthGate() {
  // ...
  if (session && inAuthGroup) {
    router.replace('/(tabs)/')  // CHANGE THIS to '/(main)/'
  }
}

// Root Stack currently has:
<Stack.Screen name="(tabs)" />  // CHANGE THIS to name="(main)"
```

From lawh-mobile/app/(tabs)/_layout.tsx (current tab layout -- will be replaced):
```typescript
export default function TabsLayout() {
  return (
    <Tabs screenOptions={{ headerShown: false }}>
      <Tabs.Screen name="index" options={{ title: 'Mushaf' }} />
      <Tabs.Screen name="hifz" options={{ title: 'Hifz' }} />
      // ...
    </Tabs>
  )
}
```

From lawh-mobile/components/mushaf/PageNavigator.tsx (current -- has internal timer):
```typescript
interface PageNavigatorProps {
  currentPage: number
  onPageChange: (page: number) => void
  onOpenSurahList: () => void  // REMOVE this -- surah list accessed from Contents screen now
}
// Has internal AUTO_HIDE_DELAY = 3000, visible state, opacity animation
// Must be refactored to accept external visible prop
```

From lawh-mobile/components/mushaf/MushafScreen.tsx:
```typescript
// Currently renders: PagerView/FlatList + PageNavigator + SurahListModal + AyahActionSheet
// PageNavigator is called with onOpenSurahList={handleOpenSurahList}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route restructuring and useChromeToggle hook</name>
  <files>
    lawh-mobile/app/(main)/_layout.tsx,
    lawh-mobile/app/(main)/index.tsx,
    lawh-mobile/app/(main)/hub.tsx,
    lawh-mobile/app/(main)/contents.tsx,
    lawh-mobile/app/(main)/hifz.tsx,
    lawh-mobile/app/(main)/recite.tsx,
    lawh-mobile/app/(main)/review.tsx,
    lawh-mobile/app/(main)/profile.tsx,
    lawh-mobile/app/(main)/settings.tsx,
    lawh-mobile/app/_layout.tsx,
    lawh-mobile/hooks/useChromeToggle.ts
  </files>
  <action>
  1. Create `lawh-mobile/hooks/useChromeToggle.ts`:
     - `useChromeToggle(autoHideMs = 5000)` hook returning `{ visible, toggle, resetTimer, show, hide }`
     - `visible` starts as `true` (app starts with chrome visible per user decision)
     - `toggle()` flips visibility; if turning ON, starts auto-hide timer
     - `resetTimer()` clears existing timer and starts new 5s countdown to hide
     - Auto-hide timer fires after `autoHideMs` and sets `visible = false`
     - Cleanup clears timer on unmount
     - Use `useState` + `useRef` + `useCallback` + `useEffect` (NOT zustand -- ephemeral UI state)
     - Use `useFocusEffect` from expo-router to reset timer when screen regains focus (per Research pitfall #2)

  2. Create `lawh-mobile/app/(main)/_layout.tsx`:
     - Import `Stack` from `expo-router`
     - Default export renders `<Stack screenOptions={{ headerShown: false, animation: 'default' }}>`
     - Register screens: index, hub, contents, hifz, recite, review, profile, settings
     - No tab bar -- pure Stack navigation

  3. Create `lawh-mobile/app/(main)/index.tsx`:
     - Move the current content from `lawh-mobile/app/(tabs)/index.tsx` here
     - This renders MushafScreen (the existing component)

  4. Create placeholder screens for hub, contents, hifz, recite, review, profile, settings in `lawh-mobile/app/(main)/`:
     - Each is a simple View with the screen name text and a "Back to Mushaf" button using `router.back()`
     - Hub and Contents will be fully built in Plan 02/03; these are minimal stubs so routes resolve

  5. Update `lawh-mobile/app/_layout.tsx`:
     - Change `<Stack.Screen name="(tabs)" />` to `<Stack.Screen name="(main)" />`
     - In AuthGate, change `router.replace('/(tabs)/')` to `router.replace('/(main)/')`
     - Everything else stays the same (fonts, seeding, auth subscription, QueryClient)

  6. Delete the old `lawh-mobile/app/(tabs)/` directory (all files: _layout.tsx, index.tsx, hifz.tsx, recite.tsx, review.tsx, profile.tsx)

  IMPORTANT: Do NOT add chrome overlay to MushafScreen yet -- that is Plan 02. This task only creates the hook and route structure.
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - App route structure uses (main) Stack layout with no tab bar
    - AuthGate redirects to /(main)/ after login
    - useChromeToggle hook exists with auto-hide timer logic
    - All screen routes resolve (index, hub, contents, hifz, recite, review, profile, settings)
    - Old (tabs) directory is removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor PageNavigator to controlled mode</name>
  <files>lawh-mobile/components/mushaf/PageNavigator.tsx</files>
  <action>
  Refactor PageNavigator to be a controlled component that receives visibility from its parent:

  1. Change `PageNavigatorProps` interface:
     - REMOVE `onOpenSurahList` prop (surah list will be accessed from Contents screen via chrome overlay icon)
     - KEEP `currentPage: number` and `onPageChange: (page: number) => void`
     - The component no longer manages its own visibility -- parent controls render via conditional

  2. Remove ALL internal visibility management:
     - Remove `visible` state
     - Remove `opacity` Animated.Value
     - Remove `hideTimer` ref and `resetHideTimer` callback
     - Remove `AUTO_HIDE_DELAY` constant
     - Remove `handleTap` callback (no tap-to-show -- parent chrome toggle handles this)
     - Remove `useEffect` that calls `resetHideTimer`
     - Remove the outer `<Pressable onPress={handleTap}>` wrapper (the `touchArea` style)

  3. Simplify render:
     - Remove the `<Animated.View style={[..., { opacity }]}>` wrapper -- just use a plain `<View>`
     - Remove the hamburger icon button (`onOpenSurahList` Pressable) -- menu icon moves to chrome overlay in Plan 02
     - Keep: slider track, slider thumb, page number display, PanResponder for scrubbing
     - Keep all slider logic (RTL, panResponder, sliderFraction calculation)

  4. Update styles:
     - Keep `container`, `sliderTrack`, `sliderThumb`, `pageNumber` styles
     - Remove `touchArea` and `iconButton` styles
     - Container should still be positioned absolutely at bottom (position: 'absolute', bottom: 0, left: 0, right: 0) with paddingBottom for home indicator

  5. Keep `React.memo` wrapper for performance.
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - PageNavigator accepts only currentPage and onPageChange props
    - No internal visibility state, timer, or opacity animation
    - Hamburger icon removed (moved to chrome overlay)
    - Slider scrubbing still works (PanResponder retained)
    - Component is a simple controlled render with no self-managed show/hide
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- App launches to mushaf screen (no tab bar visible)
- Navigating to /hub, /contents routes works (shows placeholder)
- AuthGate redirects correctly after login
</verification>

<success_criteria>
- Tab bar is completely gone from mushaf screen
- (main) Stack layout is the primary navigation structure
- useChromeToggle hook is ready for consumption by MushafScreen
- PageNavigator is a simple controlled component
</success_criteria>

<output>
After completion, create `.planning/phases/06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary/06-01-SUMMARY.md`
</output>
