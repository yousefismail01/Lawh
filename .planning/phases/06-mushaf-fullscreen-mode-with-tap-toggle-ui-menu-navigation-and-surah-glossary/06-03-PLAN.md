---
phase: 06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - lawh-mobile/app/(main)/hub.tsx
  - lawh-mobile/app/(main)/contents.tsx
  - lawh-mobile/components/contents/SurahRow.tsx
  - lawh-mobile/components/contents/JuzSectionHeader.tsx
  - lawh-mobile/components/contents/JuzIndex.tsx
  - lawh-mobile/components/contents/QuartersTab.tsx
  - lawh-mobile/components/contents/ContentsTabBar.tsx
  - lawh-mobile/lib/data/contentsData.ts
autonomous: true
requirements:
  - FSCR-04
  - FSCR-05

must_haves:
  truths:
    - "User can see all 114 surahs grouped by juz with PART N section headers"
    - "User can tap a surah row to navigate to that surah's starting page in the mushaf"
    - "User can tap a juz number in the right-edge index to scroll the list to that juz"
    - "User can switch between Surahs and Quarters tabs"
    - "User can navigate from mushaf to Home hub and see cards for all app features"
    - "User can navigate back to mushaf from hub or contents"
  artifacts:
    - path: "lawh-mobile/app/(main)/contents.tsx"
      provides: "Full Contents screen with Surahs and Quarters tabs"
      contains: "SectionList"
    - path: "lawh-mobile/app/(main)/hub.tsx"
      provides: "Home hub page with feature cards"
      contains: "FEATURES"
    - path: "lawh-mobile/lib/data/contentsData.ts"
      provides: "Juz-grouped surah sections builder"
      exports: ["buildJuzSections"]
    - path: "lawh-mobile/components/contents/SurahRow.tsx"
      provides: "Individual surah row in Contents list"
      exports: ["SurahRow"]
    - path: "lawh-mobile/components/contents/JuzIndex.tsx"
      provides: "Vertical juz 1-30 quick-scroll index"
      exports: ["JuzIndex"]
  key_links:
    - from: "lawh-mobile/app/(main)/contents.tsx"
      to: "lawh-mobile/lib/data/contentsData.ts"
      via: "buildJuzSections import"
      pattern: "buildJuzSections"
    - from: "lawh-mobile/app/(main)/contents.tsx"
      to: "lawh-mobile/lib/data/mushafData.ts"
      via: "chapters data for surah info"
      pattern: "chapters"
    - from: "lawh-mobile/components/contents/SurahRow.tsx"
      to: "expo-router"
      via: "router.replace to navigate to mushaf page"
      pattern: "router\\.(replace|push|back)"
    - from: "lawh-mobile/components/contents/JuzIndex.tsx"
      to: "lawh-mobile/app/(main)/contents.tsx"
      via: "onSelectJuz callback triggers scrollToLocation"
      pattern: "scrollToLocation"
---

<objective>
Build the full Home Hub page and the Contents screen (surah glossary) with juz-grouped SectionList, vertical juz index, Surahs/Quarters tabs, and bottom tab bar with placeholders.

Purpose: These are the two navigation destinations from the mushaf chrome overlay. The Hub replaces the tab bar as the central app navigation point. The Contents screen replaces the old SurahListModal with a much richer full-page surah glossary matching the design reference aesthetic.

Output: Fully functional Hub and Contents screens with all navigation wiring.
</objective>

<execution_context>
@/Users/yousef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yousef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary/06-CONTEXT.md
@.planning/phases/06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary/06-RESEARCH.md
@.planning/phases/06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary/06-01-SUMMARY.md

<interfaces>
<!-- From codebase -- data sources for Contents screen -->

From lawh-mobile/lib/data/mushafData.ts:
```typescript
export const chapters: Record<number, {
  nameArabic: string
  nameSimple: string
  versesCount: number
  revelationPlace: string  // 'makkah' | 'madinah'
  bismillahPre: boolean
}> = { 1: {...}, 2: {...}, ..., 114: {...} }
```

From lawh-mobile/lib/data/pageJuzHizb.ts:
```typescript
export interface PageJuzHizb {
  juz: number
  hizb: number
  quarter: number  // 0-3
}
export function getPageJuzHizb(page: number): PageJuzHizb
// Also has: JUZ_START_PAGES (30 entries), HIZB_START_PAGES (60 entries), RUB_START_PAGES (240 entries)
// These are not exported but can be if needed
```

From lawh-mobile/types/quran.ts:
```typescript
export interface Surah {
  id: number
  nameArabic: string
  nameTransliteration: string
  nameEnglish: string
  ayahCount: number
  revelationType: 'Meccan' | 'Medinan'
  revelationOrder: number
  pageStart: number
  pageEnd: number
  bismillahPre: boolean
}
```

From lawh-mobile/services/quranService.ts:
```typescript
export const quranService = {
  async getAllSurahs(): Promise<Surah[]>,
  async getSurahStartPage(surahId: number): Promise<number>,
}
```

From lawh-mobile/app/(main)/ route structure (created in Plan 01):
- index.tsx = mushaf (router.back() from hub/contents returns here)
- hub.tsx = home hub (placeholder from Plan 01, to be fully built)
- contents.tsx = surah glossary (placeholder from Plan 01, to be fully built)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build data layer and Contents screen components</name>
  <files>
    lawh-mobile/lib/data/contentsData.ts,
    lawh-mobile/lib/data/pageJuzHizb.ts,
    lawh-mobile/components/contents/SurahRow.tsx,
    lawh-mobile/components/contents/JuzSectionHeader.tsx,
    lawh-mobile/components/contents/JuzIndex.tsx,
    lawh-mobile/components/contents/QuartersTab.tsx,
    lawh-mobile/components/contents/ContentsTabBar.tsx
  </files>
  <action>
  1. Update `lawh-mobile/lib/data/pageJuzHizb.ts`:
     - Export the existing `JUZ_START_PAGES`, `HIZB_START_PAGES`, and `RUB_START_PAGES` arrays (they are currently `const` but not exported)
     - Add `export` keyword to each array declaration
     - No other changes to the file

  2. Create `lawh-mobile/lib/data/contentsData.ts`:
     - Import `chapters` from `@/lib/data/mushafData`
     - Import `getPageJuzHizb` from `@/lib/data/pageJuzHizb`
     - Define and export `SurahInfo` interface: `{ id: number, nameArabic: string, nameSimple: string, versesCount: number, revelationPlace: string, pageStart: number }`
     - Define and export `JuzSection` interface: `{ title: string, juz: number, data: SurahInfo[] }`
     - Export `buildJuzSections(): JuzSection[]` function:
       - Iterate chapters 1-114
       - For each chapter, determine its juz using `getPageJuzHizb(pageStart)` where pageStart comes from the chapters data (need to know each surah's start page)
       - NOTE: `chapters` from mushafData does NOT have pageStart. Use a lookup: surah 1 starts on page 1, surah 2 on page 2, etc. The exact start pages can be derived from the mushafData `getPageLines()` by scanning for surah line entries. Alternatively, hardcode a `SURAH_START_PAGES` array (114 entries) derived from the QPC data. The simplest approach: iterate pages 1-604, find surah banner lines, record first occurrence of each surah.
       - Actually, the `quranService.getSurahStartPage()` is async (DB query). Since this is for a SectionList that needs synchronous data, create a static `SURAH_START_PAGES: number[]` constant by scanning `getPageLines()` from mushafData for all 604 pages at module load time, or hardcode the known values. Pre-compute at build time is better: iterate pages 1-604 once, find the page where each surah first appears.
       - Better approach: use `getPageLines()` to scan pages and find surah start pages. Cache this computation in a module-level variable (computed once on first call).
       - Group surahs by juz number (from `getPageJuzHizb(surahStartPage)`)
       - Return array of `JuzSection` sorted by juz (1-30), each with `title: "PART {juz}"`, `juz` number, and `data` array of surahs in that juz
     - Export `buildQuarterSections()` function for the Quarters tab:
       - Import `HIZB_START_PAGES` and `RUB_START_PAGES` from pageJuzHizb
       - Group by juz (30 sections), each juz has 2 hizbs, each hizb has 4 quarters
       - Return structure: `{ juz: number, hizbs: { hizbNumber: number, quarters: { quarterIndex: number, startPage: number }[] }[] }[]`

  3. Create `lawh-mobile/components/contents/SurahRow.tsx`:
     - Props: `{ surah: SurahInfo, onSelect: (pageStart: number) => void }`
     - Layout per user decision: number in circle (left), transliterated name + page number + verse count + Meccan/Medinan (middle), no Arabic name (different from SurahListModal -- Contents screen matches the reference image style)
     - Number circle: 28x28 circle with border, surah number centered inside
     - Name: `surah.nameSimple` in 15px semibold
     - Subtitle line: `{versesCount} Verses - {revelationPlace}` in 12px secondary color
     - Right side: page number in secondary color
     - Cream/warm color scheme matching mushaf aesthetic (`#faf3e0` bg tones, `#6b5c3a` secondary text, `#1a1a1a` primary text)
     - `Pressable` with `onPress={() => onSelect(surah.pageStart)}`
     - `React.memo` wrapped

  4. Create `lawh-mobile/components/contents/JuzSectionHeader.tsx`:
     - Props: `{ title: string }` (e.g., "PART 1")
     - Styled as a section divider: uppercase text, 11px, letterSpacing 1.5, secondary color, horizontal line
     - Cream background matching the Contents screen

  5. Create `lawh-mobile/components/contents/JuzIndex.tsx`:
     - Props: `{ onSelectJuz: (juz: number) => void, totalJuz?: number }`
     - Renders a vertical column of numbers 1-30
     - Positioned absolutely on the right edge of the screen
     - Each number is a small Pressable (tapping calls `onSelectJuz(juz)`)
     - Numbers should be small enough (10-11px) to fit all 30 in the available height
     - Semi-transparent background pill/strip for readability
     - `React.memo` wrapped

  6. Create `lawh-mobile/components/contents/QuartersTab.tsx`:
     - Props: `{ onSelectPage: (page: number) => void }`
     - Renders hizb/quarter breakdown grouped by juz
     - Uses `buildQuarterSections()` from contentsData
     - Each juz section shows its 2 hizbs, each hizb shows 4 quarter entries with start page
     - Tapping a quarter navigates to that page in the mushaf
     - SectionList or FlatList with section headers
     - Cream/warm aesthetic matching Surahs tab

  7. Create `lawh-mobile/components/contents/ContentsTabBar.tsx`:
     - Props: `{ activeTab: string, onTabChange: (tab: string) => void }`
     - Bottom tab bar for the Contents screen with 4 tabs: Contents (active), Khatmah (placeholder), Bookmarks (placeholder), Highlights (placeholder)
     - Only "Contents" tab is functional -- the other 3 show a "Coming Soon" indicator
     - Positioned at the bottom of the Contents screen
     - Simple row of 4 Pressable items with text labels
     - Active tab has underline or different color
     - `React.memo` wrapped
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - contentsData.ts exports buildJuzSections() that groups 114 surahs into 30 juz sections
    - All Contents screen components exist and type-check
    - SurahRow, JuzSectionHeader, JuzIndex, QuartersTab, ContentsTabBar are React.memo wrapped
    - pageJuzHizb.ts exports the start page arrays
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble Contents screen and build Home Hub page</name>
  <files>
    lawh-mobile/app/(main)/contents.tsx,
    lawh-mobile/app/(main)/hub.tsx
  </files>
  <action>
  1. Build `lawh-mobile/app/(main)/contents.tsx` (replace Plan 01 placeholder):
     - Full-page Contents screen (not modal) with Surahs and Quarters tabs
     - Top area: Back button (left arrow or "Back") + title "Contents" + toggle tabs ("Surahs" | "Quarters")
       - Toggle tabs: two pill-shaped buttons, active one is filled, inactive is outlined
       - Use `useState<'surahs' | 'quarters'>('surahs')` for active tab
     - Use `useSafeAreaInsets()` for top/bottom padding
     - Back navigation: `router.back()` or right-swipe gesture (handled by Stack navigator automatically)

     **Surahs tab (default):**
     - `SectionList` with `ref` for programmatic scrolling
     - `sections` from `useMemo(() => buildJuzSections(), [])`
     - `renderSectionHeader` renders `JuzSectionHeader`
     - `renderItem` renders `SurahRow`
     - `stickySectionHeadersEnabled={false}`
     - On surah select: navigate to mushaf page by calling `router.replace('/')` with the page parameter, OR use a shared store/navigation param. Simplest approach: `router.back()` to return to mushaf, then trigger page change. Better: use `router.replace({ pathname: '/', params: { page: surah.pageStart } })` and handle the param in MushafScreen. Actually simplest: use settingsStore's `setLastReadPage(page)` then `router.back()` -- mushaf will read lastReadPage on focus.
     - On surah select handler: `const handleSelectSurah = (pageStart: number) => { useSettingsStore.getState().setLastReadPage(pageStart); router.back() }` -- this sets the page in the persisted store, then navigates back. MushafScreen already reads `lastReadPage` from settingsStore. The PagerView will need to respond to lastReadPage changes -- check if MushafScreen syncs `currentPage` with `lastReadPage`. If not, MushafScreen will need a `useFocusEffect` to re-sync. Add a comment about this.
     - `JuzIndex` rendered absolutely on the right edge, overlaying the SectionList
     - `onSelectJuz` handler: uses `sectionListRef.current?.scrollToLocation({ sectionIndex, itemIndex: 0, animated: true })` (per Research pattern #5)
     - Bottom: `ContentsTabBar` with active tab state. When non-Contents tab tapped, show "Coming Soon" text in the content area.

     **Quarters tab:**
     - Renders `QuartersTab` component (built in Task 1)
     - On quarter select: same pattern as surah select (setLastReadPage + router.back)

     **Styling:**
     - Cream/warm background (#faf3e0 for light mode)
     - Clean typography matching the reference image aesthetic
     - Color scheme support using `useColorScheme()`

  2. Build `lawh-mobile/app/(main)/hub.tsx` (replace Plan 01 placeholder):
     - Full Home Hub page -- the central navigation point replacing the tab bar
     - Import `useRouter` from `expo-router`, `useSafeAreaInsets` from safe-area-context
     - Top area: "Back to Mushaf" button (left arrow + text) at top-left, using `router.back()`
     - Title: "Lawh" or app name/logo area
     - Feature cards in a grid or list layout:
       ```
       const FEATURES = [
         { key: 'hifz', title: 'Hifz', subtitle: 'Track your memorization', icon: '📖', route: '/(main)/hifz' },
         { key: 'recite', title: 'Recite', subtitle: 'Practice recitation', icon: '🎙', route: '/(main)/recite' },
         { key: 'review', title: 'Review', subtitle: 'Spaced repetition', icon: '🔄', route: '/(main)/review' },
         { key: 'profile', title: 'Profile', subtitle: 'Your progress', icon: '👤', route: '/(main)/profile' },
         { key: 'settings', title: 'Settings', subtitle: 'App preferences', icon: '⚙', route: '/(main)/settings' },
       ]
       ```
       NOTE: Use Text-based icons or simple shapes instead of emoji if emoji look inconsistent across platforms. Use the same cream/warm aesthetic.
     - Each card: Pressable with rounded corners, subtle shadow, title + subtitle, navigates via `router.push(route)`
     - Layout: 2-column grid for cards on larger screens, single column on smaller. Use `flexWrap: 'wrap'` or manual 2-column layout
     - ScrollView wrapper for the page content
     - "Back to Mushaf" prominent card at top or as header action
     - Cream/warm color scheme (#faf3e0 bg, #6b5c3a accent)
     - Per user decision: "Full hub page built in this phase (not a placeholder)"
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Contents screen shows all 114 surahs grouped by juz with "PART N" headers
    - Tapping a surah navigates back to mushaf at that surah's page
    - Juz index on right edge scrolls to correct juz section
    - Surahs/Quarters tab toggle works
    - Bottom tab bar shows Contents (active) + 3 placeholder tabs
    - Hub page shows all 5 feature cards (Hifz, Recite, Review, Profile, Settings)
    - Hub "Back to Mushaf" navigation works
    - Both screens use cream/warm aesthetic matching mushaf
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Contents screen renders 114 surahs in 30 juz groups
- Tapping surah Al-Kahf (18) navigates to mushaf page 293
- Juz index tap scrolls to correct section
- Quarters tab shows hizb/quarter breakdown
- Hub page renders all feature cards
- Back navigation from both screens returns to mushaf
</verification>

<success_criteria>
- Contents screen is a full-page surah glossary with juz grouping, matching the reference design aesthetic
- Hub page is a complete navigation center with cards for all app features
- Navigation flows: mushaf -> hub -> back, mushaf -> contents -> surah tap -> mushaf at page
- All placeholder tabs clearly marked as "Coming Soon"
</success_criteria>

<output>
After completion, create `.planning/phases/06-mushaf-fullscreen-mode-with-tap-toggle-ui-menu-navigation-and-surah-glossary/06-03-SUMMARY.md`
</output>
