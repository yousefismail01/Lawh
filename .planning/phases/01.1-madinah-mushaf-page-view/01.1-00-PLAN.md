---
phase: 01.1-madinah-mushaf-page-view
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - lawh-mobile/__tests__/db/words.test.ts
  - lawh-mobile/__tests__/db/quranService.test.ts
  - lawh-mobile/__tests__/components/mushafPage.test.ts
  - lawh-mobile/__tests__/stores/settingsStore.test.ts
  - lawh-mobile/__tests__/components/mushafBismillah.test.ts
  - lawh-mobile/__tests__/components/mushafSpecialPage.test.ts
autonomous: true
requirements: [MUSH-01, MUSH-02, MUSH-03, MUSH-04, MUSH-05, MUSH-06]

must_haves:
  truths:
    - "All 6 test stub files exist and are runnable with jest"
    - "Each test file contains at least one describe block with pending/todo tests for the target requirement"
    - "npx jest --testPathPattern mushaf runs without errors (tests may be skipped/todo but not crashing)"
  artifacts:
    - path: "lawh-mobile/__tests__/db/words.test.ts"
      provides: "Test stubs for MUSH-01 (word schema + seeding)"
      contains: "describe"
    - path: "lawh-mobile/__tests__/db/quranService.test.ts"
      provides: "Test stubs for MUSH-02 (word queries)"
      contains: "describe"
    - path: "lawh-mobile/__tests__/components/mushafPage.test.ts"
      provides: "Test stubs for MUSH-03 (line grouping)"
      contains: "describe"
    - path: "lawh-mobile/__tests__/stores/settingsStore.test.ts"
      provides: "Test stubs for MUSH-04 (persistence)"
      contains: "describe"
    - path: "lawh-mobile/__tests__/components/mushafBismillah.test.ts"
      provides: "Test stubs for MUSH-05 (bismillah rendering)"
      contains: "describe"
    - path: "lawh-mobile/__tests__/components/mushafSpecialPage.test.ts"
      provides: "Test stubs for MUSH-06 (special pages 1-2)"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all 6 VALIDATION.md requirements so that implementation plans have functional Nyquist verification.

Purpose: VALIDATION.md identifies 6 test files that must exist before implementation begins. Without these, all plan `<verify>` tags fall back to `tsc --noEmit` which provides no functional coverage.
Output: 6 runnable test stub files with describe blocks and todo/pending tests for each requirement.
</objective>

<execution_context>
@/Users/yousef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yousef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-madinah-mushaf-page-view/01.1-CONTEXT.md
@.planning/phases/01.1-madinah-mushaf-page-view/01.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 6 test stub files with pending tests per requirement</name>
  <files>lawh-mobile/__tests__/db/words.test.ts, lawh-mobile/__tests__/db/quranService.test.ts, lawh-mobile/__tests__/components/mushafPage.test.ts, lawh-mobile/__tests__/stores/settingsStore.test.ts, lawh-mobile/__tests__/components/mushafBismillah.test.ts, lawh-mobile/__tests__/components/mushafSpecialPage.test.ts</files>
  <action>
Create 6 test stub files under `lawh-mobile/__tests__/`. Create `db/` and `components/` subdirectories if they don't exist.

Each file should:
- Import from the module it will eventually test (use the path alias `@/`)
- Have a `describe` block named after the feature
- Contain `it.todo()` stubs for the behaviors that implementation plans will fill in

**1. `__tests__/db/words.test.ts` (MUSH-01):**
```typescript
describe('words table schema', () => {
  it.todo('words table has required columns: surahId, ayahNumber, riwayah, position, pageNumber, lineNumber, textUthmani, charType')
  it.todo('seedWords inserts word records into the database')
  it.todo('seedWords skips seeding if words already exist')
  it.todo('seedWords reports progress during seeding')
})
```

**2. `__tests__/db/quranService.test.ts` (MUSH-02):**
```typescript
describe('quranService mushaf queries', () => {
  it.todo('getWordsByPage returns words ordered by lineNumber then position')
  it.todo('getWordsByPage filters by riwayah')
  it.todo('getPageMetadata returns surahs and juz for a given page')
  it.todo('isWordSeeded returns false when no words exist')
  it.todo('getSurahStartPage returns the first page of a surah')
})
```

**3. `__tests__/components/mushafPage.test.ts` (MUSH-03):**
```typescript
describe('MushafPage component', () => {
  it.todo('renders exactly 15 lines per page')
  it.todo('groups words by lineNumber into lines')
  it.todo('renders surah banner when new surah starts on page')
  it.todo('renders ayah end markers as ornamental circles')
  it.todo('wraps content in MushafFrame with ornamental border')
})
```

**4. `__tests__/stores/settingsStore.test.ts` (MUSH-04):**
```typescript
describe('settingsStore persistence', () => {
  it.todo('persists lastReadPage across store recreation')
  it.todo('persists navigationMode across store recreation')
  it.todo('defaults lastReadPage to 1')
  it.todo('defaults navigationMode to horizontal')
  it.todo('hydration guard prevents flash of default values')
})
```

**5. `__tests__/components/mushafBismillah.test.ts` (MUSH-05):**
```typescript
describe('MushafBismillah', () => {
  it.todo('renders bismillah text for standard surahs')
  it.todo('renders nothing for surah 1 (Al-Fatiha)')
  it.todo('renders nothing for surah 9 (At-Tawbah)')
  it.todo('uses centered layout')
})
```

**6. `__tests__/components/mushafSpecialPage.test.ts` (MUSH-06):**
```typescript
describe('MushafPage special pages', () => {
  it.todo('page 1 (Al-Fatiha) uses centered layout')
  it.todo('page 2 (Al-Baqarah start) uses centered layout')
  it.todo('page 3+ uses standard justified layout')
  it.todo('special pages have extra padding and bolder borders')
})
```

Note: These are `it.todo()` stubs only. They will be filled in by implementation plan executors as they build each feature. The stubs establish the test file structure so `<verify>` commands in Plans 01-03 can reference real test paths.
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-mobile && npx jest --testPathPattern mushaf --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 test stub files exist, jest runs them without errors, and each contains describe blocks with todo tests for its target requirement.</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern mushaf --no-coverage` runs without crashes
- All 6 files exist under `lawh-mobile/__tests__/`
- Each file has at least one `describe` block with `it.todo()` stubs
</verification>

<success_criteria>
- Wave 0 test infrastructure is in place
- Implementation plans (01-03) can reference test files in their `<verify>` tags
- VALIDATION.md wave_0_complete can be set to true after this plan executes
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-madinah-mushaf-page-view/01.1-00-SUMMARY.md`
</output>
