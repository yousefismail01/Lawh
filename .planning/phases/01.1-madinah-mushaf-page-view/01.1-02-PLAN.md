---
phase: 01.1-madinah-mushaf-page-view
plan: 02
type: execute
wave: 2
depends_on: [01.1-01]
files_modified:
  - lawh-mobile/components/mushaf/MushafLine.tsx
  - lawh-mobile/components/mushaf/MushafPage.tsx
  - lawh-mobile/components/mushaf/MushafFrame.tsx
  - lawh-mobile/components/mushaf/MushafPageHeader.tsx
  - lawh-mobile/components/mushaf/MushafSurahBanner.tsx
  - lawh-mobile/components/mushaf/MushafBismillah.tsx
  - lawh-mobile/components/mushaf/AyahMarker.tsx
  - lawh-mobile/hooks/useMushafPage.ts
  - lawh-mobile/assets/mushaf/frame-light.svg
  - lawh-mobile/assets/mushaf/surah-banner.svg
  - lawh-mobile/assets/mushaf/ayah-marker.svg
autonomous: true
requirements: [MUSH-03, MUSH-05, MUSH-06]

must_haves:
  truths:
    - "MushafPage renders exactly 15 lines of Arabic text matching word-level line assignments"
    - "Words within each line are ordered RTL with space-between justification"
    - "Ayah end markers render as ornamental circles with ayah numbers"
    - "Surah headers render as ornamental banners when a new surah starts on a page"
    - "Bismillah renders centered between surah header and first ayah, excluded for surah 1 and 9"
    - "Pages 1 and 2 render with special centered layout"
    - "Ornamental frame surrounds each page with cream/parchment background"
    - "Page header shows surah name, juz number, and page number"
  artifacts:
    - path: "lawh-mobile/components/mushaf/MushafPage.tsx"
      provides: "Full mushaf page: frame + header + 15 lines"
      exports: ["MushafPage"]
    - path: "lawh-mobile/components/mushaf/MushafLine.tsx"
      provides: "Single line of RTL justified words"
      exports: ["MushafLine"]
    - path: "lawh-mobile/components/mushaf/AyahMarker.tsx"
      provides: "Ornamental verse-end circle with number"
      exports: ["AyahMarker"]
    - path: "lawh-mobile/hooks/useMushafPage.ts"
      provides: "Hook that fetches + groups words by line for a given page"
      exports: ["useMushafPage"]
  key_links:
    - from: "lawh-mobile/hooks/useMushafPage.ts"
      to: "lawh-mobile/services/quranService.ts"
      via: "quranService.getWordsByPage + getPageMetadata"
      pattern: "quranService\\.getWordsByPage"
    - from: "lawh-mobile/components/mushaf/MushafPage.tsx"
      to: "lawh-mobile/hooks/useMushafPage.ts"
      via: "useMushafPage hook"
      pattern: "useMushafPage"
---

<objective>
Build all mushaf rendering components: the page, lines, ornamental elements, and the data hook that powers them.

Purpose: These components are the visual building blocks of the mushaf. Plan 03 will wire them into the PagerView navigation.
Output: Complete MushafPage component that renders a single page of the Madinah Mushaf with all ornamental elements.
</objective>

<execution_context>
@/Users/yousef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yousef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-madinah-mushaf-page-view/01.1-CONTEXT.md
@.planning/phases/01.1-madinah-mushaf-page-view/01.1-RESEARCH.md
@.planning/phases/01.1-madinah-mushaf-page-view/01.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From lawh-mobile/types/mushaf.ts:
```typescript
export interface Word {
  id: number; surahId: number; ayahNumber: number; riwayah: string;
  position: number; pageNumber: number; lineNumber: number;
  textUthmani: string; charType: string; // 'word' | 'end'
}
export interface MushafPageData {
  pageNumber: number;
  lines: Word[][];
  surahs: { id: number; nameArabic: string }[];
  juz: number;
}
```

From lawh-mobile/services/quranService.ts:
```typescript
// New methods from Plan 01:
quranService.getWordsByPage(page: number, riwayah?: Riwayah): Promise<Word[]>
quranService.getPageMetadata(page: number, riwayah?: Riwayah): Promise<{pageNumber, surahs, juz}>
```

From lawh-mobile/components/quran/AyahText.tsx:
```typescript
// Established patterns:
// fontFamily: 'KFGQPCHafs', writingDirection: 'rtl', lineHeight: fontSize * 2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useMushafPage hook and atomic mushaf components (AyahMarker, MushafLine, MushafBismillah, MushafSurahBanner, MushafPageHeader, MushafFrame)</name>
  <files>lawh-mobile/hooks/useMushafPage.ts, lawh-mobile/components/mushaf/AyahMarker.tsx, lawh-mobile/components/mushaf/MushafLine.tsx, lawh-mobile/components/mushaf/MushafBismillah.tsx, lawh-mobile/components/mushaf/MushafSurahBanner.tsx, lawh-mobile/components/mushaf/MushafPageHeader.tsx, lawh-mobile/components/mushaf/MushafFrame.tsx</files>
  <action>
1. Create `lawh-mobile/hooks/useMushafPage.ts`:
   - Accept `pageNumber: number` and optional `riwayah`
   - Call `quranService.getWordsByPage(pageNumber, riwayah)` and `quranService.getPageMetadata(pageNumber, riwayah)`
   - Group words by `lineNumber` using a Map (lines 1-15)
   - Detect surah transitions: check if consecutive words have different `surahId` -- identify which lines contain surah headers
   - Return: `{ lines: Word[][], metadata: PageMetadata, loading: boolean, error: string | null }`
   - Use `useState` + `useEffect` for data fetching (no react-query, keep simple)
   - Memoize the groupByLine function

2. Create `lawh-mobile/components/mushaf/AyahMarker.tsx`:
   - Renders an ornamental circle with the ayah number inside
   - Props: `number: string` (the verse end marker text from the API, which contains the Arabic numeral)
   - Use a View with rounded corners (circular), border in gold/dark-gold color (#c9a84c)
   - Arabic numeral text inside centered
   - Size: 22x22 on standard pages
   - fontFamily: 'KFGQPCHafs' for the number text

3. Create `lawh-mobile/components/mushaf/MushafLine.tsx`:
   - Props: `words: Word[]`, `isCentered?: boolean` (for bismillah lines and special pages)
   - Render words in `flexDirection: 'row-reverse'` (RTL) with `justifyContent: 'space-between'`
   - When `isCentered`, use `justifyContent: 'center'` with `gap: 6`
   - For each word: if `charType === 'end'`, render `<AyahMarker>`, else render `<Text>` with KFGQPCHafs font
   - Font size: 20 for standard pages (constant `MUSHAF_FONT_SIZE`)
   - lineHeight: `MUSHAF_FONT_SIZE * 2` (established pattern)
   - Fixed height per line to maintain 15-line grid
   - writingDirection: 'rtl' on each Text element

4. Create `lawh-mobile/components/mushaf/MushafSurahBanner.tsx`:
   - Props: `surahName: string` (Arabic name)
   - Renders an ornamental rectangular banner with the surah name centered
   - Background: subtle gradient or solid decorative color (#f5e6c8 light, #3a2f1f dark)
   - Border: decorative border with rounded corners and Islamic-inspired edges (use borderWidth + borderColor + borderRadius)
   - Surah name in KFGQPCHafs font, centered, slightly larger text
   - The banner should span the full width within the frame padding
   - Fixed height matching one line slot in the 15-line grid

5. Create `lawh-mobile/components/mushaf/MushafBismillah.tsx`:
   - Props: `surahId: number`
   - If surahId === 1 (Al-Fatiha) or surahId === 9 (At-Tawbah), render nothing (return null)
   - Otherwise render "بِسْمِ ٱللَّهِ ٱلرَّحْمَـٰنِ ٱلرَّحِيمِ" centered
   - Use KFGQPCHafs font, centered text, slightly smaller than body text
   - Fixed height matching one line slot

6. Create `lawh-mobile/components/mushaf/MushafPageHeader.tsx`:
   - Props: `surahName: string, juz: number, pageNumber: number`
   - Renders a header bar above the text area
   - Left side (in RTL context, visually right): surah name in Arabic
   - Right side (visually left): juz number (جُزْءُ + Arabic numeral)
   - Center: page number
   - Use a subtle separator line below
   - Small font size (12-14), muted color

7. Create `lawh-mobile/components/mushaf/MushafFrame.tsx`:
   - Props: `children: ReactNode`
   - Wraps content in an ornamental border
   - Cream/parchment background color (#faf3e0 for light mode)
   - Decorative border: double-line border with inner and outer lines, gold-tinted (#c9a84c outer, #e8d5a3 inner)
   - Corner decorations: use borderRadius + additional View elements for subtle corner flourishes
   - Padding inside frame for text area
   - Keep it simple but elegant -- can be refined later (Claude's discretion per CONTEXT.md)
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All atomic mushaf components created and compile. useMushafPage hook fetches and groups word data by line.</done>
</task>

<task type="auto">
  <name>Task 2: Create MushafPage composite component with special pages 1-2 handling</name>
  <files>lawh-mobile/components/mushaf/MushafPage.tsx</files>
  <action>
1. Create `lawh-mobile/components/mushaf/MushafPage.tsx`:
   - Props: `pageNumber: number`
   - Uses `useMushafPage(pageNumber)` hook to get lines and metadata
   - Renders complete page structure:
     a. `<MushafFrame>` wrapping everything
     b. `<MushafPageHeader>` at top with surah name, juz, page number
     c. 15 lines of content, where each line is one of:
        - A `<MushafSurahBanner>` if a new surah starts on that line (detect by checking if words on that line have a different surahId than previous line, AND the first word's position === 1 and ayahNumber === 1)
        - A `<MushafBismillah>` line (detect by looking at the line data -- the quran.com API marks these)
        - A `<MushafLine words={lineWords}>` for normal text lines

   - **Special pages 1 and 2 handling:**
     - Detect with `if (pageNumber === 1 || pageNumber === 2)`
     - Use centered layout for all text lines (`isCentered={true}`)
     - Add extra vertical spacing/padding around text
     - Larger ornamental frame elements (more padding, bolder border)
     - Page 1 (Al-Fatiha): centered surah banner, centered bismillah (as part of ayah 1 per mushaf convention), centered ayah lines
     - Page 2 (Al-Baqarah start): centered surah banner, centered bismillah, then text lines

   - **Surah transition detection within a page:**
     - A page can contain the end of one surah and the start of another
     - When a new surah starts mid-page, the line assignments from the API already account for this
     - Use the surahId change between consecutive words to detect surah transitions
     - Insert MushafSurahBanner and MushafBismillah at the appropriate line positions

   - Loading state: show a subtle shimmer/placeholder while useMushafPage loads
   - Error state: show minimal error text

   - Performance: wrap in `React.memo` to prevent re-renders when PagerView scrolls
  </action>
  <verify>
    <automated>cd /Users/yousef/Documents/Projects/Lawh/lawh-mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>MushafPage renders a complete mushaf page with frame, header, 15 lines, surah banners, bismillah, and special page 1-2 layout. Component is memoized.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- MushafPage imports and uses all atomic components
- useMushafPage hook connects to quranService
- Special pages 1-2 have distinct centered layout
- Bismillah excluded for surah 1 and 9
</verification>

<success_criteria>
- All mushaf components render without type errors
- MushafPage composes all sub-components into a complete page
- Line grouping produces exactly the lines from the word data (up to 15 per page)
- Special handling for pages 1-2, surah transitions, and bismillah
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-madinah-mushaf-page-view/01.1-02-SUMMARY.md`
</output>
