---
phase: 08-hifz-tracker-and-review-scheduler-with-adaptive-spaced-repetition
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - lawh-mobile/app/(main)/review.tsx
  - lawh-mobile/app/(main)/hifz.tsx
  - lawh-mobile/components/session/GradeBar.tsx
  - lawh-mobile/components/session/SessionSummary.tsx
  - lawh-mobile/components/mushaf/AyahActionSheet.tsx
autonomous: false
requirements: [REVW-02, REVW-03, SESS-01, SESS-02, HIFZ-04]

must_haves:
  truths:
    - "User can start a review session from the Dashboard review badge"
    - "Review session displays mushaf text with the target ayah blurred/hidden"
    - "User taps Reveal to show the hidden ayah text"
    - "After revealing, 4-button grade bar appears (Again/Hard/Good/Easy)"
    - "Grading triggers SM-2+ update and shows new interval briefly before advancing"
    - "Post-session summary shows ayahs reviewed, accuracy breakdown, and strength changes"
    - "User can start a New Memorization session for unmemorized ayahs"
    - "New Memorization session shows ayah text fully visible for study"
    - "User can mark ayahs as memorized from the mushaf long-press AyahActionSheet"
  artifacts:
    - path: "lawh-mobile/app/(main)/review.tsx"
      provides: "Review session screen with blur-reveal-grade loop"
      min_lines: 100
    - path: "lawh-mobile/app/(main)/hifz.tsx"
      provides: "New Memorization session screen"
      min_lines: 60
    - path: "lawh-mobile/components/session/GradeBar.tsx"
      provides: "4-button Again/Hard/Good/Easy grade bar"
      min_lines: 30
    - path: "lawh-mobile/components/session/SessionSummary.tsx"
      provides: "Post-session summary modal"
      min_lines: 40
  key_links:
    - from: "lawh-mobile/app/(main)/review.tsx"
      to: "lawh-mobile/stores/hifzStore.ts"
      via: "gradeAyah action on grade submission"
      pattern: "gradeAyah"
    - from: "lawh-mobile/app/(main)/review.tsx"
      to: "lawh-mobile/services/hifzService.ts"
      via: "getReviewQueue for session ayahs"
      pattern: "getReviewQueue"
    - from: "lawh-mobile/components/mushaf/AyahActionSheet.tsx"
      to: "lawh-mobile/stores/hifzStore.ts"
      via: "markMemorized/markInProgress actions"
      pattern: "useHifzStore"
---

<objective>
Build the Review and New Memorization session screens with the blur-reveal-grade loop, grade bar, post-session summary, and integrate "Mark as Memorized" into the existing AyahActionSheet.

Purpose: This completes the core hifz loop: track progress (Plan 02) -> review with grading (this plan) -> SM-2+ updates (Plan 01). Users can now do structured memorization and review sessions.

Output: Functional review session, new memorization session, AyahActionSheet with hifz actions.
</objective>

<execution_context>
@/Users/yousef/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yousef/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-hifz-tracker-and-review-scheduler-with-adaptive-spaced-repetition/08-CONTEXT.md
@.planning/phases/08-hifz-tracker-and-review-scheduler-with-adaptive-spaced-repetition/08-01-SUMMARY.md
@.planning/phases/08-hifz-tracker-and-review-scheduler-with-adaptive-spaced-repetition/08-02-SUMMARY.md

@lawh-mobile/components/mushaf/AyahActionSheet.tsx
@lawh-mobile/components/mushaf/V4AyahText.tsx
@lawh-mobile/app/(main)/review.tsx
@lawh-mobile/app/(main)/hifz.tsx

<interfaces>
<!-- Types and stores from Plans 01 and 02 -->

From lawh-mobile/lib/sr/types.ts:
```typescript
export type Grade = 0 | 2 | 3 | 5
export type HifzStatus = 'not_started' | 'in_progress' | 'memorized' | 'needs_review'
export interface ReviewCard { easeFactor: number; interval: number; repetitions: number; dueDate: string; consecutiveCorrect: number }
export interface SM2Result { easeFactor: number; interval: number; repetitions: number; dueDate: string; consecutiveCorrect: number; strengthScore: number }
export interface ReviewQueueItem { surahId: number; ayahNumber: number; dueDate: string; intervalDays: number; easeFactor: number; repetitions: number; strengthScore: number; status: HifzStatus }
```

From lawh-mobile/stores/hifzStore.ts:
```typescript
export const useHifzStore: {
  gradeAyah: (surahId: number, ayahNumber: number, riwayah: string, grade: Grade) => void
  markMemorized: (surahId: number, ayahNumber: number, riwayah: string) => void
  markInProgress: (surahId: number, ayahNumber: number, riwayah: string) => void
  loadReviewQueue: (riwayah: string) => void
  sessionAyahs: ReviewQueueItem[]
}
```

From lawh-mobile/services/hifzService.ts:
```typescript
export function getReviewQueue(riwayah: string): ReviewQueueItem[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review session screen with blur-reveal-grade flow</name>
  <files>lawh-mobile/app/(main)/review.tsx, lawh-mobile/components/session/GradeBar.tsx, lawh-mobile/components/session/SessionSummary.tsx</files>
  <action>
    1. Create `components/session/GradeBar.tsx`:
       - 4 horizontal buttons: Again (red), Hard (orange), Good (green), Easy (blue)
       - Each button shows the grade label and the projected next interval (e.g., "Again\n1d", "Good\n6d")
       - Props: `onGrade: (grade: Grade) => void`, `projections: Record<Grade, string>` (pre-computed intervals for display)
       - Medium haptic on press via expo-haptics
       - Styled with buildColors(isDark) pattern
       - React.memo wrapped

    2. Create `components/session/SessionSummary.tsx`:
       - Modal that shows after session completes
       - Props: `visible`, `onClose`, `results: { ayahsReviewed: number, grades: { again: number, hard: number, good: number, easy: number }, avgStrengthBefore: number, avgStrengthAfter: number, totalTime: number }`
       - Shows: ayahs reviewed count, grade breakdown (colored counts), strength change (before -> after), time elapsed
       - "Done" button closes modal and navigates back
       - Dark mode support

    3. Rewrite `app/(main)/review.tsx` as the Review session screen:
       - On mount: load review queue via `hifzService.getReviewQueue('hafs')`. If empty, show "All caught up!" and back button.
       - Session state: `currentIndex`, `revealed`, `sessionResults[]`, `startTime`
       - For each ayah in the queue:
         a. Show ayah location header: "Surah X, Ayah Y" with Arabic surah name from quranService
         b. Render the ayah text using `V4AyahText` component (imports from mushaf components)
         c. Initially, overlay a `BlurView` (expo-blur, intensity 80) on the ayah text area, with a centered "Reveal" button on top
         d. When user taps "Reveal": animate blur away (fade out via Animated.View opacity), set revealed=true
         e. After reveal: show GradeBar below the text
         f. On grade: call `useHifzStore.getState().gradeAyah(surahId, ayahNumber, 'hafs', grade)`, briefly flash the new interval/next date (500ms), then advance to next ayah
       - Progress bar at top: "3 / 15 ayahs"
       - When all ayahs done: show SessionSummary modal
       - Back button in header to exit session early (with confirmation alert)
       - Batch limit: if queue > 20, only load first 20 with "Continue" option at end
       - Track session timing for summary
  </action>
  <verify>
    <automated>cd lawh-mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Review session loads due ayahs, displays each with blur overlay, reveals on tap, shows grade bar, processes SM-2+ grades, and displays post-session summary.</done>
</task>

<task type="auto">
  <name>Task 2: New Memorization session and AyahActionSheet hifz actions</name>
  <files>lawh-mobile/app/(main)/hifz.tsx, lawh-mobile/components/mushaf/AyahActionSheet.tsx</files>
  <action>
    1. Rewrite `app/(main)/hifz.tsx` as the New Memorization session screen:
       - Accepts route params: `surahId` and optional `startAyah` (for launching from tracker or mushaf)
       - If no params: show a surah picker (simple FlatList of surahs from quranService.getAllSurahs()) to select what to memorize
       - Once surah selected: load all unmemorized ayahs for that surah via hifzService
       - Session flow for each ayah:
         a. Show ayah text fully visible (V4AyahText, no blur)
         b. Below: "I've memorized this" button
         c. On button press: call `useHifzStore.getState().markInProgress(surahId, ayahNumber, 'hafs')`, creates initial review_schedule entry (due tomorrow), advance to next ayah
         d. Light haptic on button press
       - Progress bar: "Learning: 3 / 10 ayahs"
       - When done: show brief summary (ayahs learned, first review due tomorrow), navigate back
       - Back button with confirmation if mid-session

    2. Add hifz actions to `AyahActionSheet.tsx`:
       - Import useHifzStore
       - Add two new action buttons in the action list (after existing actions):
         - "Mark as Memorized" (Ionicons: checkmark-circle-outline, green) -- calls hifzStore.markMemorized(surahId, ayahNumber, 'hafs'), shows brief toast/checkmark, light haptic
         - "Mark for Review" (Ionicons: repeat-outline, blue) -- calls hifzStore.markInProgress(surahId, ayahNumber, 'hafs'), shows brief toast, light haptic
       - Both close the action sheet after marking
       - If ayah already has that status, show the current status label instead of the action
  </action>
  <verify>
    <automated>cd lawh-mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>New Memorization session lets user study unmemorized ayahs and mark them for review. AyahActionSheet has "Mark as Memorized" and "Mark for Review" actions integrated with hifzStore.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete hifz tracking and session system on device</name>
  <action>Human verifies the full hifz system end-to-end on device.</action>
  <what-built>Complete hifz tracking and session system: 114-surah grid with status colors, per-ayah detail sheet, review session with blur-reveal-grade flow, new memorization session, AyahActionSheet hifz actions, SM-2+ grading with interval updates.</what-built>
  <how-to-verify>
    1. Open the app and navigate to Hub > Hifz tab
    2. Verify: 114 surah cards in a 5-column grid, all showing "not started" (neutral color)
    3. Verify: Stats panel at top shows "0 / 6,236 ayahs memorized"
    4. Long-press any ayah in the mushaf > tap "Mark as Memorized" > verify haptic + sheet closes
    5. Return to Hifz tab > verify that surah now shows "in progress" color and stats updated
    6. Tap the surah card > verify half-height bottom sheet with per-ayah strength bars
    7. Go to Dashboard tab > verify review badge shows "1 ayah due for review" (after marking + waiting or adjusting date)
    8. Tap the review badge > verify review session opens with blurred ayah text
    9. Tap "Reveal" > verify text unblurs and 4-button grade bar appears
    10. Tap "Good" > verify brief interval flash and advance to next ayah (or summary if last)
    11. Verify post-session summary shows correct stats
    12. Navigate to Hifz tab > tap a surah > tap "Start Review" from detail sheet
    13. Test New Memorization: navigate to hifz screen, select a surah, verify ayahs shown with "I've memorized this" button
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `cd lawh-mobile && npx tsc --noEmit` -- no type errors
- `cd lawh-mobile && npx jest lib/sr/sm2plus.test.ts --no-coverage` -- SM-2+ tests still pass
- Review session loads queue, blurs text, reveals, grades, shows summary
- New Memorization session loads unmemorized ayahs, marks them
- AyahActionSheet shows hifz actions
- All screens support dark mode
</verification>

<success_criteria>
- Review session: blur-reveal-grade loop works for all due ayahs
- Grade submission triggers SM-2+ computation and updates SQLite + store
- Post-session summary displays correct stats
- New Memorization session allows studying and marking ayahs
- AyahActionSheet has "Mark as Memorized" and "Mark for Review" actions
- Human verification confirms full flow works on device
</success_criteria>

<output>
After completion, create `.planning/phases/08-hifz-tracker-and-review-scheduler-with-adaptive-spaced-repetition/08-03-SUMMARY.md`
</output>
